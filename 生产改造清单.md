# 点餐智能体 — 生产环境改造清单

> 日期：2026-05-17
> 仅供学习参考，API Key 已脱敏

---

## 总览

| 等级 | 问题数 | 说明 |
|:----:|:------:|------|
| **P0** | 3 | 不改会出事 |
| **P1** | 4 | 迟早会踩坑 |
| **P2** | 3 | 长期维护会疼 |

---

## P0 — 不改会出事

### P0-1：密码硬编码

**文件**：`tools/db_tool.py`

**问题代码**：

```python
self.password = os.getenv("MYSQL_PASSWORD", "13714526535")
```

第二个参数是**默认值**，环境变量读不到就用硬编码密码，一推 git 就泄露。

**改成**：

```python
self.password = os.getenv("MYSQL_PASSWORD")
if not self.password:
    raise ValueError("MYSQL_PASSWORD 未配置，请在 .env 中设置")
```

**额外注意**：`.env` 里的变量名是 `MYSQL_USER_PASSWORD`，代码读的是 `MYSQL_PASSWORD`，名字对不上，所以环境变量没生效。统一成 `MYSQL_PASSWORD`。

---

### P0-2：多用户并发串记忆

**文件**：`api/main.py`

**问题代码**：

```python
default_agent = init_agent(tools=tools)  # 全局单例

@app.post("/chat")
def chat_with_agent(request: ChatRequest):
    thread_id = _session_thread_ids.get("default")  # 所有人都用同一个
```

所有用户共享同一个 `thread_id`，A 说"我要辣的"，B 说"我要清淡的"，下一轮可能看到对方的消息。

**改成**（方案一：每次新会话）：

```python
@app.post("/chat")
def chat_with_agent(request: ChatRequest):
    config = {"configurable": {"thread_id": str(uuid.uuid4())}}
    result = default_agent.invoke(
        {"messages": [("human", request.message)]},
        config=config
    )
```

**改成**（方案二：按用户隔离）：

```python
@app.post("/chat")
def chat_with_agent(request: ChatRequest):
    user_id = request.headers.get("X-User-Id", "anonymous")
    config = {"configurable": {"thread_id": user_id}}
    result = default_agent.invoke(
        {"messages": [("human", request.message)]},
        config=config
    )
```

---

### P0-3：没有全局异常兜底

**文件**：`api/main.py`

**问题代码**：

```python
except Exception as e:
    return {"success": False, "response": "", "error": str(e)}
```

高德超时 / Pinecone 挂了 → 用户直接看到 Python 错误信息。

**改成**：加全局异常拦截器

```python
from fastapi import Request
from fastapi.responses import JSONResponse
import logging

@app.exception_handler(Exception)
async def global_exception_handler(request: Request, exc: Exception):
    logging.error(f"未捕获异常: {exc}", exc_info=True)
    return JSONResponse(
        status_code=200,
        content={
            "success": False,
            "response": "",
            "error": "服务暂时不可用，请稍后重试"
        }
    )
```

同时每个 @tool 函数内部也套 try/except，不让异常冒到 API 层。

---

## P1 — 迟早会踩坑

### P1-1：API Key 裸奔 + 没有 .gitignore

**文件**：项目根目录（新建）

`.env` 里有真实的 `DASHSCOPE_API_KEY`、`MYSQL_USER_PASSWORD`、`PINECONE_API_KEY`，如果 `git add .` 就全传上去了。

**改成**：建两个文件

`.gitignore`：

```gitignore
.env
__pycache__/
*.pyc
venv/
myenv/
.idea/
```

`.env.example`（给其他人当模板）：

```
# 高德地图
AMAP_API_KEY=your_amap_api_key
MERCHANT_LONGITUDE=114.171248
MERCHANT_LATITUDE=22.566222
DELIVERY_RADIUS=5000.0
DEFAULT_PATH_MODE=2

# LLM
DASHSCOPE_API_KEY=your_dashscope_api_key
DASHSCOPE_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_MODE=deepseek-v4-pro

# Pinecone
PINECONE_API_KEY=your_pinecone_api_key
PINECONE_ENV=us-east-1

# MySQL
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=your_password
MYSQL_DATABASE=smart_cat
```

---

### P1-2：配置散落在各个文件

现在每个文件自己读环境变量，改个数据库地址要翻 3 个文件。

**改成**：建 `config.py`，所有配置集中管理

```python
import os
from dotenv import load_dotenv

load_dotenv()


class Config:
    # 高德地图
    AMAP_API_KEY = os.getenv("AMAP_API_KEY")
    MERCHANT_LONGITUDE = os.getenv("MERCHANT_LONGITUDE")
    MERCHANT_LATITUDE = os.getenv("MERCHANT_LATITUDE")
    DELIVERY_RADIUS = float(os.getenv("DELIVERY_RADIUS", 5000))

    # LLM
    DASHSCOPE_API_KEY = os.getenv("DASHSCOPE_API_KEY")
    DASHSCOPE_API_BASE = os.getenv("DASHSCOPE_API_BASE",
                                    "https://dashscope.aliyuncs.com/compatible-mode/v1")
    LLM_MODE = os.getenv("LLM_MODE", "deepseek-v4-pro")

    # Pinecone
    PINECONE_API_KEY = os.getenv("PINECONE_API_KEY")
    PINECONE_ENV = os.getenv("PINECONE_ENV", "us-east-1")

    # MySQL
    MYSQL_HOST = os.getenv("MYSQL_HOST", "localhost")
    MYSQL_PORT = int(os.getenv("MYSQL_PORT", 3306))
    MYSQL_USER = os.getenv("MYSQL_USER", "root")
    MYSQL_PASSWORD = os.getenv("MYSQL_PASSWORD")
    MYSQL_DATABASE = os.getenv("MYSQL_DATABASE", "smart_cat")
```

各文件改用：

```python
# 改前
os.getenv("MYSQL_PASSWORD", "13714526535")

# 改后
from config import Config
Config.MYSQL_PASSWORD
```

---

### P1-3：没有 requirements.txt

**文件**：项目根目录（新建）

```txt
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
mysql-connector-python>=9.0.0
pinecone>=7.3.0
dashscope>=1.14.0
langchain>=1.0.7
langchain-openai>=0.1.0
langgraph>=1.0.0
langchain-community>=0.1.0
langchain-text-splitters>=0.1.0
python-dotenv>=1.0.0
requests>=2.31.0
```

```bash
pip install -r requirements.txt
```

---

### P1-4：llm_tool.py 语法错误

**文件**：`tools/llm_tool.py`

**问题**：

```python
self.model.as_retriever()        # ChatModel 没有 retriever 方法
ChatPromptTemplate.from_template({...})  # 参数需要字符串，不是字典
```

**改成**：如果没人用这个文件，直接删除 `llm_tool.py`，在 `mcp.py` 里统一管理 LLM 调用。

```python
# mcp.py 顶部加
from langchain_openai import ChatOpenAI
from config import Config

_llm = None

def get_llm():
    global _llm
    if _llm is None:
        _llm = ChatOpenAI(
            model=Config.LLM_MODE,
            api_key=Config.DASHSCOPE_API_KEY,
            base_url=Config.DASHSCOPE_API_BASE,
        )
    return _llm
```

---

## P2 — 长期维护会疼

### P2-1：日志不统一

现在各文件各自开 logger，`amap_tool.py` 还用 `print()`，生产环境里 print 不进日志文件。

**改成**：建 `logger.py`

```python
import logging
import sys

def setup_logger(name: str) -> logging.Logger:
    logger = logging.getLogger(name)
    if not logger.handlers:
        handler = logging.StreamHandler(sys.stdout)
        handler.setFormatter(logging.Formatter(
            '%(asctime)s | %(name)s | %(levelname)s | %(message)s'
        ))
        logger.addHandler(handler)
        logger.setLevel(logging.INFO)
    return logger
```

各文件：

```python
# 改前
import logging
logging.basicConfig(...)
logger = logging.getLogger(__name__)

# 改后
from logger import setup_logger
logger = setup_logger(__name__)
```

`amap_tool.py` 的 `print()` 全部换成 `logger.error()` / `logger.warning()`。

---

### P2-2：重试装饰器没用到任何 tool 上

`connection_retry.py` 写了一个很好的 `with_retry_and_fallback`，但没有任何实际工具函数在用。

**改成**：在 `pinecone_tool.py` 的 `search_menu` 上套装饰器

```python
from smart_dian_can.tools.connection_retry import with_retry_and_fallback

class PineconeVectorDB:
    @with_retry_and_fallback(
        max_retries=3,
        service_name="Pinecone搜索",
        fallback_data={"results": []}
    )
    def search_menu(self, query: str, top_k: int = 3) -> dict:
        # ... 原有代码不变 ...
```

效果：Pinecone 偶尔超时自动重试 3 次，都失败则降级返回空结果，不抛异常。

---

### P2-3：数据模型两套

`api/models.py` 和 `api/main.py` 各定义了一套菜单响应模型，字段不一致，改一个容易漏另一个。

**改成**：所有数据模型统一到 `api/models.py`

```python
# api/models.py — 统一放这里
class DishItem(BaseModel):
    id: int
    dish_name: str
    price: float
    spice_level_text: str
    # ...

class ChatRequest(BaseModel):
    message: str

class MenuListResponse(BaseModel):
    success: bool
    data: List[DishItem]
    count: int

class ChatResponse(BaseModel):
    success: bool
    response: str
    error: Optional[str] = None
```

```python
# api/main.py — 只 import，不定义
from api.models import ChatRequest, ChatResponse, MenuListResponse
```

---

## 改造优先级

```
第1步（40分钟）
├── .gitignore + .env.example           ← 防泄露
├── db_tool.py 密码改从 .env 读          ← 防泄露
├── requirements.txt                     ← 可安装
├── config.py + 各文件替换 os.getenv     ← 配置集中
├── 修或删 llm_tool.py                   ← 正确性
├── 全局异常处理                          ← 不崩
├── thread_id 隔离                       ← 不串

第2步（30分钟）
├── logger.py + 各文件切到统一 logger     ← 可排查
├── connection_retry 装饰器套到实际函数    ← 复用代码
├── api/models.py 统一                    ← 维护性
```
